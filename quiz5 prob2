def recursive_reversed(items):
    if isinstance(items, list):
        return [recursive_reversed(item) for item in reversed(items)]
    return items
items = [1, [2, 3, 4, 'yeah'], 5]
recursive_reversed(items)
